from django.apps import AppConfig

class LogoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'logo'
    verbose_name = 'Settings'  # 👈 Shu nom admin panelda chiqadi